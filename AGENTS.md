# 项目规则

## 数据库访问

- 禁止在业务代码中到处拼接 SQL。Controller、Service、UI、业务流程代码不应直接拼接或散落 SQL 语句。
- 数据库访问必须集中在明确的数据访问边界内，例如 Repository、DAO、Mapper、Gateway 或 Infrastructure 层。
- 所有 SQL 必须参数化。禁止将用户输入、外部参数或动态值通过字符串拼接直接写入 SQL。
- 常规 CRUD 优先使用 ORM 或 Query Builder，以减少重复代码并统一模型、类型和查询风格。
- 复杂查询可以手写 SQL，例如复杂 JOIN、窗口函数、报表查询或性能敏感查询，但必须放在清晰的数据访问模块、查询文件或 Repository/Mapper 方法中。
- 手写 SQL 应有明确的方法名、输入输出边界，并尽量覆盖测试；关键或性能敏感 SQL 应能被单独审查和优化。
- 数据库结构变更必须通过 migration 管理。禁止仅依赖手动修改数据库结构。
- Migration 文件应进入版本控制，并与相关代码变更一起提交。
- 事务边界应清晰，优先由应用服务或用例层统一控制，避免在多个底层方法中随意开启或提交事务。
- 测试用例粒度应适中，既要聚焦单一可验证行为以便快速定位问题，又要避免因过度拆分导致测试冗余和维护成本增加。